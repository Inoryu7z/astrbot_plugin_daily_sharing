### v5.7.7

**💬 新增「话题发起」分享策略（群聊专用）**

**1. 💬 话题发起策略**

* 新增第 8 种分享类型 `topic`，专为群聊场景设计，解决现有分享类型群友难接话的问题。
* 通过 grok 联网搜索获取当前中文互联网正在热议的低门槛大众话题，由对话模型用人设语气发起讨论。
* 独立于主分享序列，拥有自己的时间段、目标群、LLM 和去重配置。

**2. 🧠 智能规避知识盲区**

* 搜索提示词明确要求「知识门槛低、有观点空间」的话题，避开需要游戏/番剧/数码等专业背景的圈内话题。
* 对话模型定位为「话题发起者」而非「评论员」，只做小评价不假装专家，避免深度讨论暴露知识盲区。
* 所有候选都不合人设时静默跳过本次分享，不强迫人设讨论 OOC 话题。

**3. ⚙️ 可自定义提示词**

* 话题搜索提示词和文案生成提示词均暴露为顶层配置项，用户可自行调整搜索方向和文案风格。

**4. 🔄 独立调度与去重**

* 独立的时间段随机调度，独立的目标群配置，按话题名去重（默认 7 天不重复）。
* 支持手动命令 `/分享 话题` 触发。

---

### v5.7.6

**🐛 修复智能分享 9 点额外分享 + 跨人格时间冲突**

**1. 🐛 修复 9 点整额外分享**

* 修复智能分享失败回退后，插件重载时残留任务被恢复，导致 9:00 出现额外分享。

**2. 🐛 修复跨人格时间冲突**

* LLM 只返回穿搭时段，不再返回具体分享时间点。
* 由代码在穿搭时段内随机选时，并自动避开与其他人格已注册时间冲突。
* 本人格两套穿搭的分享时间也不会靠太近。

**3. 🐛 修复日志异常**

* 修复 LLM 不再返回 share_time 后日志引用该字段导致的异常。

---

### v5.7.5

**✨ 智能分享调度系统上线**

**1. 🧠 智能分享调度**

* 新增智能分享调度系统，基于 dayflow 当日日程自动选择两套穿搭各自的最佳分享时间点。
* 替代原随机分享机制，避免某套穿搭被遗漏或分享时间过晚。

**2. ⚙️ 配置项**

* 新增 `persona_smart_share_conf` 人格级配置块：启用开关、触发时间、专用 LLM、等待 dayflow 超时、失败回退开关。
* 新增 `smart_share_llm_provider_id`，可为智能调度指定专用 LLM。

**3. 🔄 失败回退**

* LLM 超时 / dayflow 未就绪 / 解析失败时自动回退到原随机机制。
* dayflow 当日日程未生成时主动触发生成。

**4. ♻️ 重载恢复**

* 插件重载后自动检查并重新注册当日智能调度任务。

---

### v5.7.3

**🐛 修复 QQ 空间重复发送 + 配图时间不精确**

**1. 🐛 修复 QQ 空间重复发送**

* 修复插件热重载后补偿调度与恢复任务同时触发，导致 QQ 空间短时间内被重复发送。
* 新增 5 分钟防抖窗口与异步锁。

**2. 🐛 修复配图时间不精确**

* 视觉导演提示词中当前时间从只取小时改为精确到分钟。

---

### v5.7.2

**🐛 修复日程数据获取失败 + 历史记录写入报错**

**1. 🐛 修复日程数据获取失败**

* 修复 dayflow 插件热重载后 dailysharing 仍持有旧实例引用，导致日程数据返回空或错误。
* 改为每次调用时重新查找插件实例。

**2. 🐛 修复历史记录写入报错**

* 修复 `record_bot_reply_to_history()` 传入不存在的参数导致 TypeError。

---

### v5.7.1

**🐛 修复副人格无法分享 + 配置架构优化**

**1. 🐛 修复副人格无法分享**

* 修复启用 QQ 空间时崩溃并阻断其他人格任务注册。

**2. 🛡️ 人格级异常隔离**

* 单个人格任务注册失败不再阻断其他人格和全局任务。

**3. ⚙️ 配置架构调整**

* 移除全局级人格配置，所有人格统一在「多人格配置」列表中管理。
* 人格配置为空时使用硬编码默认值。

**4. 📋 日志增强**

* 关键节点日志从 debug 提升为 info。

---

### v5.7.0

**🔄 适配 QZone v4.0.0 + 修复本地图片发布**

**1. 🔄 适配 QZone v4.0.0**

* 适配 QZone 插件 v4.0.0 Daemon 架构重构。

**2. 🐛 修复本地图片发布失败**

* 修复本地图片路径未被识别为可信，导致 QQ 空间 AI 配图发布失败。

---

### v5.6.9

**🐛 修复视频发送超时误判导致重复发送**

**1. 🐛 修复视频重复发送**

* 修复 QQ NTEvent 超时（retcode=1200）时消息实际已送达却被误判为失败，导致视频被重试发送最多 3 次。

**2. 🔧 重试策略优化**

* 超时错误不再重试。
* 视频发送最大重试次数从 3 次降为 2 次。

---

### v5.6.8

**🐛 修复人格级配置全局回退失效**

**1. 🐛 修复配置回退失效**

* 修复人格级配置回退到全局时使用了错误的 key，导致全局配置永远读不到，最终回退到硬编码默认值。

---

### v5.6.7

**🐛 修复 QQ 空间定时任务从未被注册**

**1. 🐛 修复全局级 QQ 空间任务缺失**

* 修复无人格模式或全局级 QQ 空间任务永远不会被调度。

**2. 🐛 修复群聊/私聊目标检查阻断 QQ 空间**

* 修复 `has_targets` 提前 return 阻止 QQ 空间注册。

**3. 🐛 修复 enable_qzone 三值逻辑**

* 新增方法正确处理 `enable_qzone` 默认值，确保未设置时能回退到全局配置。

**4. 🔧 Schema 默认值修正**

* 修复 6 个人格级布尔字段默认值，确保未设置时能回退到全局配置。

---

### v5.6.6

**🎨 视觉导演提示词重构为自然语言段落**

**1. 🎨 提示词模板重构**

* 视觉导演提示词从 JSON 6 字段输出改为一段连贯的自然语言段落，直接作为 AI 绘画提示词。
* 禁止分类标签和词组堆砌，要求自然语言句子衔接。

**2. 🧹 代码简化**

* 删除 `_assemble_selfie_prompt()` 方法，视觉导演直接输出完整提示词。
* 简化 `_call_aiimg_selfie()` 逻辑。

---

### v5.6.5

**🛡️ 稳定性优化**

**1. 🐛 修复数据库连接泄漏**

* 修复异常时连接未关闭导致长时间运行后文件描述符耗尽。

**2. 🐛 修复 HTTP Session 未复用**

* NewsService 和 ContentService 改为共享 Session，不再每次请求新建。

**3. 🐛 修复后台任务未追踪**

* 新增统一创建并追踪后台任务的方法，确保插件卸载时所有后台任务都能被取消。

**4. 🐛 修复 QZone 并发不安全**

* 新增互斥锁，防止多个 QQ 空间分享并发时互相覆盖。

**5. 🐛 修复 LLM 降级状态竞态**

* LLM 降级状态改为每次调用独立，多人格并发分享互不干扰。

---

### v5.6.4

**🐛 TTS 人格名获取修复 + 视频发送重试**

**1. 🐛 修复 TTS 无法获取人格名**

* 修复使用 `getattr()` 访问 TypedDict 导致始终返回空字符串，TTS 被跳过。

**2. ✅ TTS 人格名兜底**

* 框架 API 无法返回人格名时，从插件多人格配置中取第一个启用的人格名作为兜底。

**3. ✅ 视频发送重试**

* 新增视频发送重试机制，失败时自动重试最多 3 次，间隔递增。

---

### v5.6.3

**🎬 视频自动保存到衣橱**

**1. 🎬 视频自动入库**

* 新增视频生成后自动保存到衣橱插件的视频库。
* 保存时关联源配图，衣橱可据此建立图片-视频关联。

---

### v5.6.2

**🛡️ 随机调度防冲突 + 配图敏感词重试**

**1. 🛡️ 随机调度防冲突**

* 新增同一个人格多个随机时间窗口的冲突检测，避免短时间内连发多条分享。

**2. 🔁 配图敏感词重试**

* 新增 AI 配图因敏感内容被拦截时自动重新生成提示词，最多重试 2 次。
* 仍失败则仅发送文案。

---

### v5.6.1

**🔊 TTS 迁移至巴巴啵一 + 视觉导演提示词修复**

**1. 🔊 TTS 插件迁移**

* 从 tts_emotion_router 迁移到 tts_plus（巴巴啵一），原生支持多人格语音路由。
* `text_to_speech` 新增 `persona_name` 参数。

**2. 🐛 修复视觉导演提示词**

* 修复旧版自定义模板使用了不存在的字段及未处理的占位符，导致输出结构异常。

---

### v5.6.0

**🚀 视频生成跟随人格链路**

**1. 🚀 人格级视频服务商**

* 视频生成支持人格级 `video_provider_ids`，未配置则降级到全局。
* 视频生成与自拍模式共享同一人格配置体系，不同人格可使用不同视频后端。

---

### v5.5.9

**🐛 修复 ImageService 缺少 plugin 属性 + 心情驱动覆盖用户配置**

**1. 🐛 修复视频提示词智能分析失效**

* 修复 `ImageService` 缺少 `plugin` 属性导致视频提示词智能分析始终降级为默认提示词。

**2. 🐛 修复心情驱动绕过用户配置**

* 修复心情候选与用户配置时间段序列无交集时返回全部硬编码候选，绕过用户配置。
* 心情驱动改为在用户配置的序列内调整权重，不再替换候选池。

---

### v5.5.8

**🎬 智能视频提示词 + 构图优化 + Bug 修复**

**1. 🎬 智能视频提示词**

* 新增 `enable_smart_video_prompt` 配置项（默认开启），使用多模态 LLM 识图生成针对性视频提示词。
* 新增 `video_director_prompt`、`video_llm_provider_id` 配置项。
* 识图失败或关闭时自动回退到默认提示词。

**2. 🎨 构图优化**

* 所有分享类型的构图指令从"中景"改为"半身"，人物占比更大。
* 视觉导演提示词新增构图要求，约束环境描述不过度展开。

**3. 🐛 Bug 修复**

* 修复图片生成失败时视频提示词错乱。
* 修复新闻图片 URL 被传入视频生成导致视频被跳过。
* 修复 `_call_aiimg_selfie` 返回 `"None"` 被当作有效文件路径。
* 修复文字内容为空时丢失图片。
* 修复 LLM 降级后无法恢复。
* 修复视频生成仅使用第一个 provider、无降级。
* 修复识图 401 错误。
* 修复指定 `provider_id` 时不应该降级到不支持图片的默认模型。
* 修复 `generate_video_from_image` 不传 `persona_name`。
* 修复全局锁导致多人格分享任务互相排斥，改为人格级锁。
* 修复视频超时后文字和图片全部丢失，改为每条消息独立 try/except。
* 修复 `_bg_tasks` 未初始化。

---

### v5.5.7

**🐛 修复 ContextService 崩溃 + 全局分享任务不启动**

**1. 🐛 修复 ContextService 崩溃**

* 修复 `ContextService` 缺少 `plugin` 属性导致生活上下文格式化时崩溃。

**2. 🐛 修复全局分享任务不启动**

* 修复多人格模式下全局配置的分享任务被跳过，导致全局人格永远不分享。

---

### v5.5.6

**✨ 人格配置默认值预填充**

**1. ✅ 默认值预填充**

* 新建人格时，所有配置项默认值与全局设置相同，而非留空回退。
* 涵盖 37 个配置项。
* 提示文本从"留空回退全局"改为"默认与全局相同，可修改"。

---

### v5.5.5

**🐛 修复自拍图不存入衣橱**

**1. 🐛 修复自拍图不入库**

* 修复 `_call_aiimg_selfie()` 生成自拍图后未触发 Wardrobe 自动存图。
* 新增方法在自拍图生成后直接调用衣橱存图入库。

---

### v5.5.4

**🐛 多人格配置 Bug 修复 + 配置界面补全**

**1. 🐛 修复去重天数配置失效**

* 修复 `dedup_days_limit` 键名与 schema 不匹配，去重天数始终使用默认值 60。

**2. 🐛 修复 LLM 配置回退**

* 修复 `llm_timeout=0` 不回退全局配置。
* 修复人格级 `llm_provider_id` 和 `llm_timeout` 配置完全无效。

**3. 🐛 修复单人格模式 trigger_mode**

* 修复无人格配置时始终使用 cron 模式。

**4. 🛠️ 配置界面补全**

* 补全全局 `basic_conf`、`qzone_conf` 缺失字段。
* 补全人格 `persona_qzone_conf`、`persona_context_conf` 缺失字段。

**5. ✅ 新增 persona_name 参数**

* `_call_llm_wrapper` 和 `format_life_context` 新增 `persona_name` 参数，支持人格级 LLM 和群聊拟人化人设开关。

---

### v5.5.3

**🐛 修复跨插件硬依赖导致加载失败**

**1. 🐛 修复加载失败**

* 移除对 `astrbot_plugin_daymind` 的硬依赖，将相关 Mixin 内联到本插件。
* 修复云服务器未安装 daymind 插件时插件无法加载。

---

### v5.5.2

**🐛 多人格模式深度 Bug 修复 + 死代码清理**

**1. 🐛 修复多人格 cron 任务互相覆盖**

* 修复 `setup_custom_target_crons` 中 job_id 不含人格前缀，多人格共享目标群时 cron 任务互相覆盖。

**2. 🐛 修复 QQ 空间配置错误**

* 修复 `execute_qzone_share` 未传递 `persona_name` 给新闻源选择。
* 修复 QQ 空间配图开关逻辑混乱。

**3. 🐛 修复多人格数据混流**

* 修复 `get_recent_history_by_target` 不按人格过滤。
* 修复 `_recover_pending_jobs` 不恢复人格级延迟任务。

**4. 🐛 修复 enable/disable 命令**

* 修复 `cmd_enable`/`cmd_disable` 不适配多人格模式，禁用会删除所有人格任务。

**5. 🧹 死代码清理**

* 移除 6 个已废弃的死代码方法。
* 修复帮助文本仍包含已移除的"知识"类型。

---

### v5.5.1

**🐛 多人格模式关键 Bug 修复**

**1. 🐛 修复服务对象崩溃**

* 修复 `ContentService` 和 `NewsService` 缺少 `plugin` 引用导致多人格模式下崩溃。

**2. 🐛 修复人格解析与防抖**

* 修复 `persona_manager` 无空值检查。
* 修复 `_get_persona_info` 解析链脆弱。
* 修复 `_last_share_time` 跨人格共享导致防抖误跳过。
* 修复 LLM 工具触发分享时不解析当前会话人格。

**3. 🛠️ 配置补全**

* 补充 `persona_basic_conf` 缺失的 `trigger_mode` 和 `sharing_cron` 字段。

---

### v5.5.0

**🎭 多人格全面支持**

**1. 🎭 多人格配置系统**

* 新增 `personas` template_list 配置，每个人格可独立覆盖所有配置项。
* 支持人格级配置回退到全局。

**2. 📦 存储层人格隔离**

* `sent_history` 和 `topic_history` 表新增 `persona_name` 字段。
* 自动迁移旧数据，向后兼容单人格配置。

**3. ⏰ 调度层人格隔离**

* 每个人格独立的定时任务、随机时间段调度、QQ 空间定时任务。
* 状态键按人格名隔离。

**4. 🧠 内容层人格隔离**

* 内容生成按人格获取人设、心情、梦境、日程。
* 配图、新闻源、生活上下文按人格获取。

**5. 📡 发送目标人格隔离**

* 每个人格可配置独立的群聊/私聊发送目标。
* 早报保持全局共享。

---

### v5.4.2

**🧹 配置界面精简**

**1. ⚙️ 移除定时模式**

* 移除 `trigger_mode`、`sharing_cron`、`cron_random_delay` 配置项（仅从 UI 移除，代码保留）。
* 移除 `qzone_trigger_mode`、`qzone_cron` 配置项。
* 默认 trigger_mode 从 `cron` 改为 `random_period`。

---

### v5.4.1

**🧹 提示词去污染 + 配图质量提升**

**1. 🧹 提示词去污染**

* life_moment 的"小吐槽"方向改为"小确幸"，避免与 rant 类型重叠。
* dream 移除 `life_hint`，避免污染梦境叙事。

**2. 🎨 配图提示词升级**

* 配图最终包装提示词升级，强调完整保留人物特征，禁止拼图。

---

### v5.4.0

**🌙 梦境分享上线 + 配置界面优化**

**1. 🆕 新增梦境分享类型**

* 新增 dream 分享类型，基于 DayMind 梦境历史生成"昨晚梦到..."的分享。
* 无梦境数据时自动降级为 mood 类型。
* 凌晨/深夜时段默认序列加入 dream。

**2. 🎨 配图适配**

* dream 类型配图：朦胧梦境风格，虚化背景，柔和光线。

**3. 🎲 心情驱动候选池更新**

* 疲惫/低落/平静心情时，dream 加入候选池。

**4. ⚙️ 配置界面优化**

* 所有类型列表加入 dream。
* 配置项重新排序：开关→定时→序列→去重。
* 顶层配置顺序重排。

---

### v5.3.0

**🧩 分享类型重构：日常碎片 & 吐槽上线，知识下线**

**1. 🆕 新增分享类型**

* 新增 life_moment（日常碎片）：基于 DayFlow 当前时段日程 + DayMind 实时心情生成。
* 新增 rant（吐槽碎碎念）：基于 DayMind 负面心情标签触发。

**2. ❌ 移除知识类型**

* 移除 `knowledge` 分享类型及相关配置。
* 上午/下午时段序列默认值从 `news, knowledge` 改为 `news, life_moment`。

**3. 🔗 DayMind / DayFlow 集成**

* 新增插件发现机制，自动查找 DayMind 和 DayFlow 插件。
* life_moment 和 rant 的文案生成会自动融合 DayMind 心情 + DayFlow 日程数据。

**4. 📸 配图适配**

* life_moment：随手拍风格。
* rant：无奈表情，生活场景。

**5. 🎲 分享类型选择策略重构**

* 从"依次轮换"改为"心情驱动 + 加权随机"混合策略。
* DayMind 有心情数据时按心情标签筛选候选类型池。
* DayMind 无数据时回退到时段序列的加权随机。

---

### v5.2.0

**📸 配图架构重构：全面接入 aiimg 自拍模式**

**1. 🤳 自拍模式全面替代**

* 配图统一走 aiimg 插件的 selfie（自拍）模式，通过参考照保持人物形象一致性。
* 每个人格使用独立配置的服务商链路。
* 自拍模式失败时直接报错，不降级到文生图。

**2. 🧹 配置精简**

* 移除 `use_gitee_selfie_ref`、`appearance_prompt`、`image_always_include_self`、`image_never_include_self`、`selfie_ref_prompt` 等不再需要的配置项。

**3. 📝 提示词优化**

* 配图提示词从逗号分隔标签改为中文自然语言描述。

---

### v5.1.1

**🔍 资料准确性与防幻觉升级**

**1. 🌐 全域联网检索**

* 新闻热搜分享时自动通过 Tavily 全网检索事件最新进展与真实细节。
* 知识和推荐类分享改为"百度百科 + Tavily 搜索"双引擎并发获取参考资料，降低幻觉概率。
* Agent 根据分享类型动态调整搜索关键词。

**2. ⚙️ 配置项**

* 新增 `启用 Tavily 检索分享内容` 开关，默认启用。

---

### v5.0.0

**🌌 沉浸感与高可用升级**

**1. 🌐 全域社交联动**

* 联动 `astrbot_plugin_qzone`，支持 LLM 工具直接调用发说说。
* 新增【每天60s读懂世界】与【AI资讯快报】独立定时任务，支持自动同步至 QQ 空间。
* 修复 AI 资讯快报源头无数据时推送空白图片的问题。

**2. 🎭 拟真化人设与认知增强**

* 新增形象参考图逻辑，基于预设参考图生成生活照，保持长相和气质一致。
* 强制 LLM 进行日程与昵称交叉比对，避免对用户本人说"我和张三吃饭"的语病。
* 新增"过往动态记忆"参考，避免车轱辘话。
* 开放【知识】与【推荐】类型的内容库自定义。

**3. ⏰ 调度与可用性重构**

* 推送目标支持"三段式"高阶语法，可为特定群设置独立的定时器和分享内容轮换。
* 随机触发时间段和 Cron 随机延迟结果存入 SQLite，Bot 重启后精准恢复未执行任务。
* 新增多时间段随机分享模式。
* LLM 节点宕机或风控时自动降级到配置中的第一个可用默认模型。

**4. ⚡️ 底层优化**

* 重写聊天历史获取逻辑，支持深度回溯（默认 24 小时）。
* 优化对其他插件实例的扫描探测逻辑。
* 新增 8 大热搜源：夸克、36氪、51CTO、A站、爱范儿、网易、新浪、澎湃。

---

### v4.0.0

**🚀 架构级升级：原生适配与性能重构**

**1. ⚡️ 异步数据库架构**

* 所有 SQLite 操作放入独立线程池执行，杜绝数据库 I/O 阻塞主线程。

**2. 🔗 原生上下文注入**

* 插件发送的分享会以"隐形对话"形式写入历史记录，Bot 能基于分享内容继续与用户深入聊天。

**3. 👁️ 深度上下文感知**

* 聊天历史获取支持深度回溯（默认 24 小时）。
* 新增群聊热度分析，Bot 据此调整开场白的打扰程度。

**4. 🤖 零配置 Bot 发现**

* 插件启动时自动扫描 AstrBot 加载的所有适配器，自动锁定可用 Bot 实例，开箱即用。

---

### v3.0.0

**🧠 核心升级：全链路 Agent 化**

**1. 🎨 配图生成重构**

* 新增思考链路，引入 JSON 结构化提取。
* 智能仲裁"文案 vs 日程"的地点冲突，减少"图文不符"。

**2. 🧠 选题与情感逻辑重构**

* 新增选题 Agent，结合历史记录"头脑风暴"出新颖的冷知识，自动去重。
* 新增情感分析 Agent，基于全文理解判断 TTS 语音情感。

**3. 🌒 细粒度时间管理**

* 新增"深夜 (Late Night)"时段（22-24点），文案强制带晚安，配图光影转为幽暗风格。

---

### v2.0.0

**⚡️ 架构升级：异步任务流**

**1. ⚡️ 异步任务流**

* 将生成与发送逻辑剥离为后台任务，解决因绘图/TTS 耗时过长导致 LLM 工具调用超时。

**2. 🎬 AI 视频生成**

* 新增将静态配图转换为 5 秒动态视频。

---

### v1.0.0

**🌱 初始发布**

**1. 📝 智能文案**

* 基于 LLM 生成，集成 Life Scheduler（天气/穿搭/日程）和聊天历史。

**2. 🎨 智能配图**

* 根据文案自动生成绘画提示词。
* 支持从生活上下文中提取"穿搭"信息，智能判断是否需要人物出镜。

**3. 🗣️ 情感语音**

* 集成 TTS 插件，根据分享类型和时间段自动注入情绪。

**4. ⏰ 定时任务**

* 支持 Cron 定时任务、时间段感知、全网热搜聚合。
