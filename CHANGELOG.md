### v5.5.1
**🐛 多人格模式关键Bug修复**

*   修复 `ContentService` 和 `NewsService` 缺少 `plugin` 引用导致多人格模式下 `AttributeError` 崩溃
*   修复 `persona_manager` 无空值检查，框架未初始化时崩溃
*   修复 `_get_persona_info` 解析链脆弱，增加空值守卫和类型兼容
*   修复 `_last_share_time` 跨人格共享导致防抖误跳过
*   修复 `async_daily_share_task`（LLM工具触发）不解析当前会话人格
*   修复 `commands.py` 冗余条件判断
*   补充 `_conf_schema.json` 中 `persona_basic_conf` 缺失的 `trigger_mode` 和 `sharing_cron` 配置字段

### v5.5.0
**🎭 多人格全面支持：每个人格独立配置、独立调度、独立内容生成**

**1. 🎭 多人格配置系统**
*   新增 `personas` template_list 配置，每个人格可独立覆盖所有配置项
*   复用 DayMind 的 `PersonaConfigMixin`，支持人格级配置回退到全局
*   人格级配置包括：分享目标、基础设置、LLM提供商、配图、TTS、QQ空间、上下文、新闻源

**2. 📦 存储层人格隔离**
*   `sent_history` 和 `topic_history` 表新增 `persona_name` 字段
*   自动迁移旧数据，向后兼容单人格配置

**3. ⏰ 调度层人格隔离**
*   每个人格独立的定时任务和随机时间段调度
*   每个人格独立的QQ空间定时任务
*   状态键按人格名隔离，互不干扰

**4. 🧠 内容层人格隔离**
*   内容生成按人格获取人设、心情、梦境、日程
*   配图按人格获取参考图和自拍链路
*   新闻源按人格选择
*   生活上下文按人格获取

**5. 📡 发送目标人格隔离**
*   每个人格可配置独立的群聊/私聊发送目标
*   早报保持全局共享

**6. 🔗 依赖变更**
*   新增硬依赖：`astrbot_plugin_daymind`（PersonaConfigMixin）

### v5.4.2
**🧹 配置界面精简：移除定时模式，仅保留随机时间段**

**1. ⚙️ 配置界面精简**
*   移除 `trigger_mode`、`sharing_cron`、`cron_random_delay` 配置项（仅从 UI 移除，代码保留）
*   移除 `qzone_trigger_mode`、`qzone_cron` 配置项（同上）
*   更新群/用户配置提示词，移除定时格式说明
*   默认 trigger_mode 从 `cron` 改为 `random_period`

### v5.4.1
**🧹 提示词去污染 + 配图质量提升**

**1. 🧹 提示词去污染**
*   life_moment 的"小吐槽"方向改为"小确幸"，避免与 rant 类型重叠
*   dream 移除 `life_hint`（当前生活状态），避免污染梦境叙事
*   rant 保留 `chat_hint`（吐槽可能由群聊话题触发）

**2. 🎨 配图最终包装 prompt 升级**
*   旧：`请根据参考图生成一张新的自拍照：1) 以第1张参考图的人脸身份为准...`
*   新：`以参考图中同一少女为基准，完整保留其五官、身材等全部人体特征，绝对禁止任何拼图，参考她的面部特征为其生成一张单人的自然生活照：她有着白皙细腻的皮肤，纤细的身姿与格外饱满的曲线形成鲜明对比，`

### v5.4.0
**🌙 梦境分享上线 + 配置界面优化**

**1. 🆕 新增梦境分享类型（dream）**
*   基于 DayMind 梦境历史生成"昨晚梦到..."的分享，调用 `get_dream_history()` 和 `get_dream_aftereffect_for_persona()` 接口。
*   无梦境数据时自动降级为 mood 类型。
*   凌晨/深夜时段默认序列加入 dream。

**2. 🎨 配图适配**
*   dream 类型配图：朦胧梦境风格，虚化背景，柔和光线。

**3. 🎲 心情驱动候选池更新**
*   疲惫/低落/平静心情时，dream 加入候选池。

**4. ⚙️ 配置界面优化**
*   所有类型列表加入 dream。
*   序列提示词从"按顺序循环执行"改为"系统会根据心情和时段智能选择"。
*   配置项重新排序：开关→定时→序列→去重，逻辑更清晰。
*   顶层配置顺序重排：总开关→目标→定时→内容库→上下文→新闻→配图→语音→空间→早报→模型。

### v5.3.0
**🧩 分享类型重构：日常碎片 & 吐槽上线，知识下线，DayMind/DayFlow 集成**

**1. 🆕 新增分享类型**
*   **日常碎片（life_moment）**：随手发的生活碎片，像朋友圈那种感觉。基于 DayFlow 当前时段日程 + DayMind 实时心情生成，每条内容都有数据支撑。
*   **吐槽碎碎念（rant）**：带点幽默的小抱怨，不是真的愤怒。基于 DayMind 负面心情标签 + reason 触发，有情绪波动时才发。

**2. ❌ 移除知识类型**
*   移除 `knowledge` 分享类型及相关配置（`knowledge_cats` 知识分类库、知识类 Tavily 搜索路由等）。
*   上午/下午时段序列默认值从 `news, knowledge` 改为 `news, life_moment`。

**3. 🔗 DayMind / DayFlow 集成**
*   新增 `_find_daymind_plugin()` / `_find_dayflow_plugin()` 插件发现机制，通过 `context.get_all_stars()` 自动查找。
*   新增 `_get_daymind_mood()` 获取当前心情标签、副标签、原因。
*   新增 `_get_dayflow_timeline_now()` 获取当前时段日程、穿搭、今日主题。
*   `life_moment` 和 `rant` 类型的文案生成会自动融合 DayMind 心情 + DayFlow 日程数据。

**4. 📸 配图适配**
*   `life_moment`：随手拍风格，生活感，自然不做作。
*   `rant`：无奈表情，生活场景。
*   默认配图允许列表更新为 `greeting, mood, life_moment, recommendation`。

**5. ⚙️ 配置更新**
*   所有时段序列提示词更新为新类型列表。
*   移除 `knowledge_cats` 配置项。
*   LLM Tool 描述更新为新类型列表。

**6. 🎲 分享类型选择策略重构**
*   从"依次轮换"改为"心情驱动 + 加权随机"混合策略。
*   DayMind 有心情数据时：按心情标签筛选候选类型池（开心→日常/推荐/问候，平静→日常/新闻/心情，负面→吐槽/心情/日常），再从候选池中加权随机选择。
*   DayMind 无数据时：回退到时段序列的加权随机（上次选过的类型降权）。
*   不再出现"永远按固定顺序发"的机械感。

### v5.2.0
**📸 配图架构重构：全面接入 aiimg 自拍模式**

**1. 🤳 自拍模式全面替代**
*   **彻底走自拍链路**：配图不再使用 draw（文生图）或 edit（通用图生图）模式，所有场景统一走 aiimg 插件的 selfie（自拍）模式，通过参考照保持人物形象一致性。
*   **人格专属服务商链路**：正确使用 `chain_override` 参数，每个人格使用独立配置的服务商链路，而非全局 edit chain。
*   **专业自拍提示词**：使用 aiimg 内置的 `_build_selfie_prompt()` 构建提示词，支持人格级别的 `prompt_prefix` 和 `default_output` 配置。
*   **零降级策略**：自拍模式失败时直接报错，不降级到文生图。

**2. 🧹 配置精简**
*   移除 `use_gitee_selfie_ref`（不再需要，始终启用自拍模式）
*   移除 `appearance_prompt`（外貌由参考照保证）
*   移除 `image_always_include_self` / `image_never_include_self`（始终包含人物）
*   移除 `selfie_ref_prompt`（改用 aiimg 内置提示词构建）

**3. 📝 提示词优化**
*   配图提示词从逗号分隔标签改为中文自然语言描述，更适配自拍模式的提示词模板。

### v5.1.1
**🔍 资料准确性与防幻觉升级：全面接入 Tavily 联网检索**

**1. 🌐 全域联网增强**
*   **彻底告别标题党**：打通 AstrBot 内置的 Tavily 网页搜索能力。开启后，在分享新闻热搜时，Bot 会自动去全网检索事件的“最新进展”与“真实细节”，拒绝仅凭标题进行苍白的脑补。
*   **双引擎并发检索**：重构了【知识】和【推荐】的底层链路。现在会采用“百度百科 + Tavily 搜索”双引擎并发获取参考资料。不仅大幅丰富了科普原理和作品评价，更从根本上降低了模型产生“幻觉”的概率。
*   **智能 Query 路由**：Agent 会根据当前生成的类型（新闻/知识/推荐），动态调整搜索关键词（如知识类侧重“原理、详细解释”，推荐类侧重“评价、核心亮点”）。

**2. ⚙️ 配置化管理**
*   在 `新闻热搜设置` 中新增 `启用 Tavily 检索分享内容` 开关，默认启用。

### v5.0.0
**🌌 沉浸感与高可用升级：生态联动与智能调度重构**

**1. 🌐 全域社交联动 (Qzone & 早报体系)**
*   **QQ空间生态打通**：深度联动 `astrbot_plugin_qzone`，支持 LLM 工具直接调用发说说。Bot 现在不仅能在群聊/私聊发声，还能拥有自己的“朋友圈”。
*   **独立早报速递**：新增【每天60s读懂世界】与【AI资讯快报】的独立定时任务。支持一键开启自动同步至 QQ 空间，支持 LLM 自然语言触发。
*   **资讯容错优化**：优化了 AI 资讯快报的获取逻辑，彻底解决源头无数据时推送空白图片的问题。

**2. 🎭 拟真化人设与认知增强**
*   **形象参考图 (图生图)**：完美适配新版 `astrbot_plugin_gitee_aiimg`，新增**形象参考图逻辑**。Bot 终于可以基于预设参考图生成生活照，保持长相和气质的绝对一致性！
*   **认知防呆机制 (第三人称修正)**：强制 LLM 进行“日程与昵称”交叉比对。若日程中包含用户昵称，自动转换为“和你一起”的第二人称语气，彻底杜绝“我今天要去和张三吃饭(对张三说)”的智障语病。
*   **动态记忆串联**：新增“过往动态记忆”参考，Bot 在生成新内容时会回顾最近发过的动态，避免车轱辘话，让生活轨迹更连贯。
*   **专属知识库自定义**：开放【知识】与【推荐】类型的内容库，用户可完全自定义话题标签池。

**3. ⏰ 工业级调度与可用性重构**
*   **千群千面独立调度 (高阶特性)**：重构了推送目标的解析引擎，现支持“三段式”高阶语法（`群/QQ号:独立Cron定时:专属类型` 或 `群/QQ号:专属类型`）。你可以为某些特定的群设置**完全独立的定时器和分享内容轮换**，彻底脱离全局规则！(例如：`123456:0 7 * * *:news,mood` 即可让该群仅在早7点按新闻、心情轮换)。
*   **防丢/防重的持久化调度**：重写定时器底座。将“随机触发时间段”和“Cron随机延迟”的生成结果统一存入 SQLite 数据库。即使 Bot 重启或崩溃，也能在启动时精准恢复未执行的延迟任务，**彻底解决重启导致的重复推送或跳过执行问题**。
*   **多时段随机阵列**：新增多时间段随机分享模式，支持统一设置或为独立群组设置专属的随机时间点，彻底告别机械的固定时间推送。
*   **LLM 自动降级熔断**：指定的大模型节点宕机或风控？系统会自动捕获并无缝降级到配置中的第一个可用默认模型，确保分享任务永不中断。

**4. ⚡️ 底层引擎与健壮性优化**
*   **深度回溯引擎重写**：采用全新的游标分页(SEQ)策略重写聊天历史获取逻辑，在群聊消息海量时抓取更稳定、更深层。
*   **插件发现机制强化**：优化了对其他插件（如 Gitee生图、Qzone）实例的扫描探测逻辑，完美兼容 AstrBot 在不同阶段的异步加载状态。
*   **海量热搜源扩充**：新增 夸克、36氪、51CTO、A站、爱范儿、网易、新浪、澎湃 8 大热搜源接入。
*   **常规维护**：修复了若干奇奇怪怪的底层 BUG。

### v4.0.0
**🚀 架构级升级：原生适配与性能重构**

**1. ⚡️ 异步数据库架构**
*   **重构**：引入数据库管理，全面重写底层存储逻辑。
*   **性能**：所有 SQLite 操作（读写历史、状态更新）均放入独立线程池执行。
*   **效果**：彻底杜绝了因数据库 I/O 导致的 Bot 主线程卡顿，即使历史记录达到数万条也能秒级响应。

**2. 🔗 原生上下文注入**
*   **对话历史注入**：适配 AstrBot 的上下文新接口。
    *   **变更前**：Bot 发送的每日分享是“孤立”的消息，不被 LLM 记忆，导致用户回复“展开讲讲”时 Bot 一脸懵逼。
    *   **变更后**：插件会伪造一组“隐形对话”（User: 请发送分享 -> Bot: [分享内容]）写入历史记录。
    *   **效果**：完美保持上下文连贯性，Bot 能基于刚才分享的内容继续与用户深入聊天。

**3. 👁️ 深度上下文感知**
*   **深度分页策略**：重写聊天历史消息获取逻辑。
    *   打破了仅获取最近 20 条消息的限制，现在支持**深度回溯**（默认24小时聊天历史消息）。
    *   采用分批轮询机制，确保在群聊消息量巨大时也能准确抓取上下文。
*   **群聊态势感知**：新增群聊热度分析算法。
    *   统计短时间内的有效消息数、活跃人数。
    *   计算 **聊天热度**：Bot 现在知道群里是“死寂”、“闲聊”还是“激烈讨论”，并据此调整开场白的打扰程度。

**4. 🤖 零配置 Bot 发现**
*   **智能初始化**：不再强制要求用户在配置文件中填写繁琐的平台适配器。
*   **逻辑**：插件启动时会自动扫描 AstrBot 加载的所有适配器，自动锁定可用的 Bot 实例。
*   **效果**：开箱即用，极大降低了新手配置门槛。

### v3.0.0
**🧠 核心升级：全链路 Agent 化**

**重构 配图生成逻辑：**
- 新增 `_agent_extract_visuals` 思考链路，引入 JSON 结构化提取。
- 智能仲裁“文案 vs 日程”的地点冲突，减少“图文不符”的幻觉。

**重构 选题与情感逻辑：**
- 新增 **选题 Agent**：结合历史记录“头脑风暴”出新颖的冷知识，自动去重。
- 新增 **情感分析 Agent**：基于全文理解判断 TTS 语音情感（happy/sad/angry）。

**🕒 细粒度时间管理**
- 新增 **“深夜 (Late Night)”** 时段（22-24点），文案强制带晚安，配图光影转为幽暗风格。

### v2.0.0 
**⚡️ 架构升级：异步任务流**
- 将生成与发送逻辑剥离为后台 `asyncio.create_task`，彻底解决因绘图/TTS耗时过长导致的 LLM 工具调用超时问题。
- **✨ 新增 AI 视频生成**：支持将生成的静态配图转换为 5秒 动态视频，让 Bot 的“早安”动起来。

### v1.0.0 
**🌱 初始发布**
- 支持 Cron 定时任务、时间段感知、多模态（图/文/音）内容生成、全网热搜聚合。

**多模态内容生成：**
- 📝 智能文案：基于 LLM 生成，集成 Life Scheduler（天气/穿搭/日程）和 聊天历史，拒绝尴尬开场。
- 🎨 智能配图：根据文案自动生成绘画提示词。支持从生活上下文中提取“穿搭”信息，智能判断是否需要人物出镜（画人 vs 画景）。
- 🗣️ 情感语音：集成 TTS 插件，根据分享类型和时间段自动注入情绪（happy/sad/neutral/angry）。